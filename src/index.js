import React from 'react';
import ReactDOM from 'react-dom/client';
import TedxPage from './components/TedxPage';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TedxPage />
  </React.StrictMode>
);
